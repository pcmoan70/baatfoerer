# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Claude
.claude/

# OS
.DS_Store
Thumbs.db

# Scraped PDFs are large and mostly off-topic; we keep only the relevant ones
# (skolekart, kartsymboler, bfp-kartsamling) via `git add -f`. The rest stay
# local — their URLs are preserved in data/*/index.json for re-fetch/linking.
data/**/*.pdf

